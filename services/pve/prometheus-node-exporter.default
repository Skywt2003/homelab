# Listen only on the PVE Tailscale address. Prometheus runs on lab and reaches
# this endpoint over the private tailnet.
ARGS="--web.listen-address=100.64.0.4:9100 --collector.systemd --collector.systemd.unit-include=(pveproxy|pvedaemon|pvestatd|pve-cluster|corosync).service"
